[project]
name = "fusioncareer-agent"
version = "0.1.0"
description = "FusionCareer AI Agent — 节点式工作流微服务"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "openai>=1.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "pdfplumber>=0.11",
    "python-dotenv>=1.0",
    "jsonschema>=4.0",
    "apscheduler>=3.10",
    "requests>=2.31",
    "python-docx>=1.1",
    "lxml>=5.0",
    "Pillow>=10.2",
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
]
ocr = [
    "paddlepaddle==3.1.1",
    "paddleocr==3.1.0",
    "paddlex>=3.1.0,<3.2.0",
    # PaddleX 3.1 imports langchain.docstore, removed in LangChain 1.x.
    "langchain>=0.2,<1.0",
    "langchain-community>=0.2,<0.4",
    "langchain-openai>=0.1,<1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.setuptools.packages.find]
include = ["app*"]
exclude = ["runtime*", "tests*"]

[tool.ruff]
line-length = 120
